"use client";

/* ─────────────────────────────────────────────────────────────
   River — Transparent spacer
   The full-page background image already shows the meadow with
   the winding creek through this region. This component just
   reserves vertical space so the next section (CTA) lands where
   the meadow widens and meets the cliff edge in the image.
   ───────────────────────────────────────────────────────────── */

export function River() {
  return <section className="relative w-full" style={{ height: 80 }} />;
}

export default River;
